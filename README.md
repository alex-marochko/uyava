# Uyava (public)

This repository currently hosts:
- `remote_config_repo/` for remote configuration served via GitHub Pages
- `site/` for the public website and docs (placeholder for now)

The main Uyava codebase will be published here later.
